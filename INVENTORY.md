# Inventory

GENERATED by `node scripts/inventory.js` — do not edit by hand.
`inventory.test.js` fails when this file drifts from the code.

## Run commands (L6 verbs)
`acquire` · `beat` · `catch` · `evolve` · `faint` · `give` · `heartScale` · `levelUp` · `nickname` · `party` · `release` · `take` · `teach`

## CLI subcommands (`play.js`)
`acquire`* · `advise` · `beat`* · `box` · `catch`* · `evolve`* · `faint`* · `find` · `give`* · `graveyard` · `heartscale`* · `learn` · `level`* · `log` · `matrix` · `milestones` · `new`* · `next` · `nickname`* · `party`* · `plan` · `rank` · `release`* · `routes` · `scout` · `split` · `status` · `take`* · `teach`* · `undo`* · `where`

\* mutates the save; unmarked are read-only.

## HTTP endpoints
- `GET /calculate`
- `POST /calculate`
- `POST /ai/validate-battle-state`
- `POST /ai/choose-action`
- `POST /ai/evaluate-actions`
- `POST /ai/apply-action`
- `POST /ai/derive-switch-entry`
- `POST /ai/forced-switch-actions`
- `POST /ai/derive-resolution`
- `POST /ai/derive-end-turn`
- `POST /ai/advance-turn`
- `POST /ai/order-actions`
- `GET /planner/fights`
- `GET /planner/fight`
- `GET /planner/upcoming`
- `POST /planner/predict`
- `POST /run/new`
- `POST /run/apply`
- `POST /run/undo`
- `POST /run/status`
- `POST /run/where`
- `POST /run/learnable`
- `POST /run/plan`
- `POST /run/routes`
- `POST /run/rank`
- `POST /run/split`
- `POST /run/matrix`
- `POST /run/advise`
- `POST /run/scout`
- `GET /run/maps`

## Browser panels
- `#runbun-battle` — Run and Bun Battle Turn Viewer
- `#runbun-planner` — Run and Bun Fight Planner
- `#runbun-run` — Run and Bun playthrough
- `#runbun-replay` — Run and Bun Replay Scrubber

## Solver layer exports
- `run.js`: `adviseCatches`, `adviseUpgrades`, `apply`, `applyAll`, `boxMatrix`, `capAt`, `createRun`, `encounterRules`, `encountersOn`, `fightTier`, `findMon`, `isExcludedVariant`, `learnable`, `levelCap`, `milestones`, `partySpecs`, `planNext`, `rankParties`, `requireLayer`, `split`, `splitPrep`, `summarize`, `undo`, `unusedRoutes`, `upcoming`
- `planner.js`: `buildFightState`, `getFight`, `holdableItem`, `listFights`, `loadRunMap`, `matchup`, `playerStateFromEntry`, `predict`, `upcoming`
- `simulate.js`: `format`, `loadRun`, `parseBorrowedTeam`, `resolveParty`, `selectFights`, `simulate`
- `team.js`: `NATURES`, `formatMon`, `formatTeam`, `parseTeam`

## Profile: run-and-bun
Layers declared: `data`, `mechanics`, `policy`, `encounters`, `oracle`, `provenance`
Trainer fights in the run map: 362

Oracle datasets:
- `availability.json`: 6 species
- `encounters.json`: 131 maps
- `evolutions.json`: 435 species
- `growth.json`: 1114 species
- `learnsets.json`: 1114 species

## Test files
`browser_calc_load.test.js` · `browser_planner.test.js` · `browser_run.test.js` · `inventory.test.js` · `planner.test.js` · `play.test.js` · `rom-band.test.js` · `run.test.js` · `runbun_oracle.test.js` · `runbun_policy.test.js` · `runbun_sets.test.js` · `runbun_species.test.js` · `server.smoke.test.js` · `sets_to_battle_state.test.js` · `simulate.test.js` · `team.test.js`

## Prior art elsewhere (from ECOSYSTEM.json)

### PhilapR/pokemon-mono
The operator's primary monorepo — four earlier repos subtree-imported (see its RECEIPTS-MAP.md). Read-only prior art for this project.
Verification: all claim paths verified

- **Heart Scale = set one IV to 31 (Run & Bun mechanic) — SOUND, PORTED** — `engines/rlm/src/pokemon_rlm/simulator/inventory.py`<br>use_heart_scale(bag, pokemon, stat): refuses at 31, consumes from bag, recalculates stats. heart_scales_to_max, iv_quality_rating. Not shop-buyable, sell 100. Graded 2026-08-11: small, correct, and the one genuinely unique capability in engines/rlm — validated by execution. Ported 2026-08-11 as the run command `heartScale`: same three refusals, spent from run.bag, and the IV feeds the planner directly because the box already stores calc spat keys.

- **Cross-engine evidence tier — GRADE runbuncalc WITH IT, NEVER MERGE IT IN** — `contracts/cross-engine`, `engines/rab/backend/tests/rom-band-acceptance.test.ts`<br>1,727 emulator-captured damage observations (fidelity/events-f*.json, self-describing, ROM-free) + a provider-agnostic engine contract. Its value is exactly that it is external — an engine that grades itself cannot catch a shared-wrong value (see rab's FINDINGS.md PP=10 episode). CASHED 2026-08-11: the three event files are vendored at profiles/run-and-bun/fidelity/ (mono commit be6570d, hashes in that README) so the gate runs without this clone, and rom-band.test.js grades this calculator against them — 1,727/1,727 = 100.00% in band, crits included. The corpus stays read-only prior art; nothing else was merged in.

- **In-battle consumable selection** — `engines/rlm/src/pokemon_rlm/agents/battle_items.py`<br>Best HP heal for a deficit, best status cure, best ball.

- **Budget-constrained shop optimizer — DO NOT PORT** — `engines/rlm/src/pokemon_rlm/simulator/shops.py`<br>optimize_purchase(budget, needs). Graded 2026-08-11: fabricates shop locations ('Verdantrum Town', marts on routes that have none) with no provenance. For item availability port engines/rab/backend/src/data/rab-item-availability.ts instead — trainer-order-keyed and cites Mechanics.MD.

- **MDP battle solver: deathless probability, expected deaths — OBJECTIVE GOOD, IMPLEMENTATION UNSOUND** — `engines/rlm/src/pokemon_rlm/analytics/mdp/mdp.py`<br>Graded 2026-08-11: the death reward is SIGN-INVERTED at four sites (reward = -death_penalty with every caller passing -100.0, so the 'optimal' policy maximises deaths), the DP pass is unordered, the state key aliases distinct states, and damage is hardcoded to level 12 with a Gen 6 crit multiplier — behind 59 type-and-range tests that catch none of it. Take the OBJECTIVE (score a fight by P(no deaths), E[deaths]); rebuild the solver fresh on runbuncalc's ai/ and calc.

- **Trainer database and team planning scaffolding** — `engines/rlm/src/pokemon_rlm/agents/team_planner.py`<br>Locations, level bands, opponent type profiles.

- **Community research-notes pipeline (Optimal Moveset extraction)** — `engines/rlm/src/pokemon_rlm/data/runandbun_research.py`<br>Structured extraction from community docs; provenance transcribed, not source-of-truth.

- **Run & Bun battle agent with cross-engine gates and calibration receipts** — `engines/rab`<br>Subtree of rab-battle-agent. Cross-engine diff gates, ROM-band acceptance tests, calibration observation logs. Unswept in detail.

- **ROM ground truth engine** — `groundtruth/pykemon`<br>Subtree of pykemon. Unswept in detail.

- **RL environment** — `learners/rl-env`<br>Subtree of pokemon-rl-env. Unswept in detail.

- **Doc-drift tooling worth copying** — `tools/check-doc-authority.py`, `docs/authority.json`<br>Rejects volatile test-count snapshots in authoritative docs via a manifest. This repo's inventory gate is the same idea, applied here.

### PhilapR/platinum-kaizo-agent
Another-game agent. Unswept — listed so it is not rediscovered by accident.
Verification: not yet cloned in this environment

### PhilapR/pk-workspace
Workspace repo, contents unknown. Unswept — listed so it is not rediscovered by accident.
Verification: not yet cloned in this environment

### PokemonLinePlanner (local copy; upstream unknown)
Turn-by-turn fight planner with snapshots — the design ancestor of this repo's Fight Planner. MatchupFinder computes best-known-move both directions.
Verification: local copy in session scratchpad 2026-08-11

### PokemonRunAndBunHelper (local copy; upstream unknown)
Python battle helper: find_best_move depth-3 search over a battle state.
Verification: local copy in session scratchpad 2026-08-11
