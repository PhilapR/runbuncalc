{
  "name": "@runbun/insight",
  "private": true,
  "version": "0.1.0",
  "description": "Reads headless run records and explains how fights were won or lost. Artifacts in, typed analysis and one HTML page out; it never imports the harness.",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit -p .",
    "build": "tsc -p .",
    "test": "npm run build && node --test dist/test/insight.test.js",
    "viewer": "npm run build && node dist/src/cli.js"
  },
  "dependencies": {
    "effect": "^3.22.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
